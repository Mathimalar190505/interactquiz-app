module.exports = {
  // This section defines which plugins PostCSS should use.
  plugins: {
    // 1. Run the Tailwind CSS processor.
    tailwindcss: {},
    // 2. Automatically add vendor prefixes (like -webkit- or -moz-) for cross-browser compatibility.
    autoprefixer: {},
  },
}
